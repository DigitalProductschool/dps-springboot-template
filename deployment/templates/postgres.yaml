apiVersion: dps.cluster/v1alpha1
kind: SQLClaim
metadata:
  name: NAME
spec:
  id: NAME
  compositionSelector:
    matchLabels:
      provider: PROVIDER
      db: postgresql
  parameters:
    version: VERSION
    size: SIZE
  writeConnectionSecretToRef:
    name: NAME
