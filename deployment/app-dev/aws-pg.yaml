apiVersion: dps.cluster/v1alpha1
kind: SQLClaim
metadata:
  name: dps-template-dev-pg
spec:
  id: dps-template-dev-pg
  compositionSelector:
    matchLabels:
      provider: aws
      db: postgresql
  parameters:
    version: "15.3"
    size: small
  writeConnectionSecretToRef:
    name: dps-template-dev-pg
